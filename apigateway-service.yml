server:
  port: 8089

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: CLIENT-SERVICE
          uri: CLIENT-SERVICE
          predicates:
            - Path=/api/client/**
